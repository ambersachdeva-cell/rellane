import { describe, expect, it } from "vitest";
import { canonicalCapabilityIntentSha256 } from "./capability-intent-binding.js";
const id=(n:number)=>`00000000-0000-4000-8000-${n.toString().padStart(12,"0")}`; const hash="a".repeat(64); const value=()=>({schemaVersion:1,permissionRequestId:id(1),permissionDecisionId:id(2),spaceId:id(3),runId:id(4),effectId:id(5),effectRevision:1,effectKind:"export-artifact",authoritySessionId:id(6),subjectBindingSha256:hash,targetBindingSha256:hash,parameterSha256:hash,requestSha256:hash,expiresAt:"2026-08-03T00:05:00.000Z",maxUses:1});
describe("private capability intent binding",()=>{it("is order-independent and rejects hostile values",()=>{const first=value(); expect(canonicalCapabilityIntentSha256(first)).toBe(canonicalCapabilityIntentSha256(Object.fromEntries(Object.entries(first).reverse()))); for(const bad of [{...first,extra:true},new Proxy(first,{getPrototypeOf:()=>{throw new Error("hostile")}})]) expect(()=>canonicalCapabilityIntentSha256(bad)).toThrow();});});
